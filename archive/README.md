# Archive

This folder preserves project materials that are useful for history or reference but are no longer part of the active, script-first workflow.

Nothing here should be required to run the main pipeline. Active execution should go through `scripts/` with configs under `configs/`.
