# Legacy Scripts

Scripts here are preserved for reference but are not part of the active final
pipeline.

`plot_analysis_supervised_baselines.py` contains the earlier Logistic Regression
and XGBoost comparison. It is archived because those baselines use labeled
failure windows for training, while the final project comparison uses a fair
unsupervised Isolation Forest baseline.
