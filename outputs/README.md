# Outputs Directory

Use this directory for local ad hoc exports that should not be committed.

Final lightweight report artifacts belong in `reports/figures/` and
`reports/tables/`. Full model runs, weights, scores, and predictions belong
under `models/` and are ignored by Git.
