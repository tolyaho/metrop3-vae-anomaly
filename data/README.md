# Data

The runnable configs in this repository use `dataset/` as the canonical local
MetroPT3 data directory. This `data/` directory is kept only as a conventional
research-project placeholder.

Do not commit raw datasets, processed arrays, or private local data here.
